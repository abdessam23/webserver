#include <stdlib.h>
#include <netdb.h>
#include <unistd.h>
#include <stdio.h>
#include <arpa/inet.h>

int main(int ac,char** arg)
{
    // char buf[10];
    // int n = read(0,buf,10);
    // printf("%s", buf);
    // return 0;
}